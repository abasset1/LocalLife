# Profil dev
# Clé API OpenAgenda (commune à tous les agendas) — externalisée (LL-9004),
# voir application.properties pour le détail.
openagenda.api-key=${OPENAGENDA_API_KEY:}

# Depuis LL-EF-005, les agendas eux-mêmes (uid, nom, filtre région) ne se
# configurent plus par propriété (l'ancien openagenda.agenda-uid a été
# retiré) mais depuis l'interface d'administration — voir la migration
# V14__add_agenda_fields_to_source.sql pour les valeurs reprises du profil
# par défaut.
